# hardware_medium.yaml — 8 GB RAM, quad-core CPU (student laptop)
# This is the primary development + evaluation tier.
# Only deltas from base.yaml are listed here.

hardware:
  tier: medium
  n_threads: 4
  n_batch: 128

model:
  name: llama-3.2-3b
  gguf_path: models/llama-3.2-3b-q4_k_m.gguf
  n_ctx: 2048
  logits_all: true
  max_new_tokens: 256
